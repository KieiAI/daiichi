[tool.poetry]
name = "daiichi-backend"
version = "0.1.0"
description = ""
authors = [
  {name = "kankankanp", email = "minamikan0825@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.115.11"
uvicorn = "^0.34.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
